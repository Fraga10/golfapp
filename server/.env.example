# Example .env for the server (copy to `server/.env` and edit values)
# The docker-compose in this folder will load variables from this file.

# Database connection the server will use (must be reachable from the container)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=golfe_db
DB_USER=golfe_user
DB_PASS=change_me

# Server port
PORT=8080

# Optional: API base url used by mobile clients when running against container
# API_BASE_URL=http://host.docker.internal:8080

# Optional Git auto-update (if you want the container to auto-pull from the repo)
# Set to 1 to enable the in-container auto-puller (requires `.git` present inside container).
# GIT_AUTO_PULL=1
# Interval in seconds to poll the remote for updates
# GIT_PULL_INTERVAL=30
# Optional: remote and branch to track (defaults to origin and current branch)
# GIT_REMOTE=origin
# GIT_BRANCH=main
